spring.application.name=lms

spring.datasource.url=jdbc:mysql://localhost:3306/institute_db
spring.datasource.username=root
spring.datasource.password=mY$pQL!58gN#9cD^7sZl

server.error.whitelabel.enabled=false


# Enable debug mode (prints additional startup details)
# debug=true

# Set logging levels to DEBUG for root, Spring, and your application packages
# logging.level.root=DEBUG
# logging.level.org.springframework=DEBUG
# logging.level.com.project.lms=DEBUG

# Enable detailed SQL logging (shows SQL queries and formatting)
# spring.jpa.show-sql=true


# Hibernate configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect


# Include error messages and binding errors in responses (for debugging)
# server.error.include-message=always
# server.error.include-binding-errors=always
